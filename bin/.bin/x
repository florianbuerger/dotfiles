#!/bin/sh

XCODE=`xcode-select -p | awk -F'/' '{print $3}'`

echo "Using $XCODE"
if ls *.xcworkspace >/dev/null 2>&1; then
    open -a "$XCODE" *.xcworkspace
elif ls *.xcodeproj >/dev/null 2>&1; then
    open -a "$XCODE" *.xcodeproj
else
    echo "Nothing here"
fi
